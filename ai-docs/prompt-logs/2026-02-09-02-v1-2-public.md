---
title: "Prompt Log — ciam-demo (Public Repo) CI/CD & v1.2.0 Release"
project: ciam-demo-private
sub_project: ciam-demo-private
type: prompt-log
date: 2026-02-09
tags: []
why_private: "contains AI session transcripts with internal development decisions"
status: stable
source_repo: https://github.com/jackzhaojin/ciam-demo-private.git
source_tool: claude-code
harvested: 2026-03-28
---

# Prompt Log — ciam-demo (Public Repo) CI/CD & v1.2.0 Release

## Session 1: CI/CD Pipeline Overhaul & v1.2.0 Release (Feb 8-9, 2026)

### Prompt 1: Tag-Only Build Triggers (earlier session)

> Hey, can you make sure that both claims-web.yml and claims-api.yml — can we only have a trigger on a tag with a version number so that we only build when there's some version tagging that's pushed to GitHub?

→ Response: Changed triggers from branch push to `push: tags: ['v*']`, updated Docker image tags from `github.sha` to `github.ref_name`.
→ Action: Updated `claims-api.yml`, `claims-web.yml`.

### Prompt 2: GitHub Releases + Retroactive Versioning (earlier session)

> Would that be too much to ask that I guess if I were tagging it, yeah. And help me think this through, since ideally I should be making a release as well with the version tagging... whatever it takes for us to retroactively do 1.0.0 and 1.1.0 correctly.

→ Response: Attempted `release: types: [published]` trigger — failed for retroactive tags (GitHub evaluates workflow at tagged commit). Switched to `push: tags` + `workflow_dispatch`.
→ Action: Multiple iterations on workflow triggers, added `version`/`ref`/`deploy` inputs.

### Prompt 3: Separate Build & Deploy (earlier session)

> Let's have the build and deploy be separate things if that's possible — maybe have an option by default we build and deploy, but can deploy only trigger off of build only?

→ Response: Added `deploy` boolean input (default true). Refactored deploy-only workflows to support `workflow_call` + `workflow_dispatch`. Build workflows call deploy-only via `uses:`.
→ Action: Updated all 4 workflow files.

### Prompt 4: Race Condition & Release-First Pattern (earlier session)

> Not all of them can be creating releases, right? Releases for the whole monorepo — do we think through that or is this a gap?

→ Response: Identified race condition with both workflows creating releases. Adopted release-first pattern: `gh release create` → tag push → triggers builds. Added `update-release` job to append container image links.
→ Action: Removed `create-release` job, added `update-release` job to both workflows.

### Prompt 5: README Documentation (earlier session)

> Can you create a README.md in '.github/workflows' so this is all clearly documented?

→ Response: Created comprehensive CI/CD documentation with architecture diagram, design decisions, how-to commands.
→ Action: Created `.github/workflows/README.md`.

### Prompt 6: Retroactive v1.0.0 & v1.1.0 Builds (earlier session)

> OK pushed, let's try the claims API now... let's build 1.0.0 and 1.1.0...

→ Response: Built both services at v1.0.0 (commit `2429574`) and v1.1.0 (commit `65c0e8b`) via workflow_dispatch. Deploy confirmed working via workflow_call.
→ Action: 4 workflow_dispatch runs, manual release note updates for v1.0.0, v1.1.0 auto-updated.

### Prompt 7: Version in Health Check Discussion (earlier session)

> The current health check — there's no way to report versions, right? How would we do that in both Java and JavaScript?

→ Response: Discussed `APP_VERSION` build arg → env var → health endpoint pattern. No changes made per user request (public repo).
→ Action: Discussion only, noted for future. ✅

### Prompt 8: v1.2.0 Release Assessment & Execution (12:03 AM)

> Alright - public repo just pushed over a bunch of commits as you can see, hopefully it has the version changes you're hoping for, and they tested it over here. I pushed to origin as well. Do some assessment, and if no issues are found, please go ahead and do the release as documented here.

→ Response: Assessed 14 commits since v1.1.0 — tests pass, workflows correct, health endpoints have version reporting, Dockerfiles have APP_VERSION. No issues. Created release v1.2.0.
→ Action: Ran tests, reviewed key files, `gh release create v1.2.0 --generate-notes`. Both workflows triggered. ✅

### Prompt 9: v1.2.0 Release Notes Gap (12:05 AM)

> We understand how come the previous 1.1.0 and the 1.2.0 are like the previous summaries of things and we just don't have any current one [screenshot of GitHub releases page showing v1.2.0 with sparse auto-generated notes vs v1.1.0/v1.0.0 with hand-written summaries]

→ Response: Identified gap — v1.0.0/v1.1.0 had hand-written summaries, v1.2.0 only had auto-generated changelog link + one container image.
→ Action: Drafted summary based on `git log v1.1.0..v1.2.0`.

### Prompt 10: How Were Previous Notes Created? (12:08 AM)

> Well yeah, let's I guess yeah you do a diff of all the stuff we did. Yeah, help me out a little bit? Don't make changes. Do not make changes. I'll meet understands first.

→ Response: Investigated — v1.0.0/v1.1.0 summaries were AI-written in a previous session via `gh release create --notes` or `gh release edit`. `--generate-notes` only gives a changelog link. `update-release` CI job only appends container image links. No automation for feature summaries.
→ Action: Research only, `gh release view` + `gh api` to inspect all 3 releases.

### Prompt 11: Update v1.2.0 Release Notes (12:12 AM)

> Well yeah, let's I guess yeah you do a diff of all the stuff we did. Yeah, go ahead and hand write write something. That's fine. And in the future I'm guessing when I create a release with that gh command, I should put in more stuff.

→ Response: Updated v1.2.0 release with proper summary (8 bullet points + container images). Noted future workflow: pass `--notes` with content, or use `gh release edit` after `--generate-notes`.
→ Action: `gh release edit v1.2.0 --notes "..."`. ✅

### Prompt 12: Health Check Both Services (12:15 AM)

> healthcheck

→ Response: Both services UP, reporting v1.2.0.
→ Action: `curl` to API :8080 and Web :3001 health endpoints. ✅

### Prompt 13: Smoke Test on Oracle (12:16 AM)

> Do some quick smoke testing on the functionality on oracle please, nothing super intense with playwright mcp (not claude chrome mcp)

→ Response: Ran Playwright smoke test — landing page, Keycloak login, OAuth callback, dashboard, org switcher, role-based nav gating, API 401 guard. Initially appeared all good.
→ Action: Playwright browser automation against 157.151.152.16:3001. Noted dashboard showed skeleton loaders.

### Prompt 14: Shared Supabase & Data Discrepancy (12:20 AM)

> mm... they're sharing the same supabase? i'm seeing content here [screenshot of localhost:3000 showing 8 claims with full analytics]

→ Response: Confirmed both local and Oracle share same Supabase instance. Oracle dashboard had shown skeleton loaders during smoke test but localhost showed full data.
→ Action: Acknowledged shared DB situation.

### Prompt 15: Triage the Skeleton Loader Issue (12:22 AM)

> ya please triage

→ Response: Attempted direct API token fetch (password grant rejected — client not configured for it). Used Playwright to re-login on Oracle — data loaded fine on second visit.
→ Action: Playwright login + inspection on Oracle.

### Prompt 16: Don't Brush This Under the Rug (12:25 AM)

> Well let's it's not for real what just happened. I know it's working now. use your best and most intelligent brain. What could have made previous result happen? Don't brush this under the rug.

→ Response: Formed two hypotheses: (1) empty state bug for orgs with no claims showing permanent skeletons, (2) post-OAuth redirect race condition where session isn't ready when dashboard mounts.
→ Action: Analysis only — queued for testing.

### Prompt 17: Do More Testing (12:27 AM)

> Do more testing, if that makes sense.

→ Response: Systematically tested both hypotheses. Key findings: (1) globex-inc shows same data as acme-corp after switch — **org switcher doesn't re-fetch data**, only changes nav. (2) Fresh login loads correctly. (3) Switching orgs keeps stale data from previous org.
→ Action: Playwright sign-out → fresh login → org switch testing. Confirmed **org switcher data refresh bug**. ✅

### Prompt 18: v1.2.1 Release Creation (12:31 AM)

> OK i just pushed whatever over, gh create 1.2.1 with a reasonable release note about bug fixing

→ Response: Created v1.2.1 release with bug fix summary (redirect loop, status filtering, vitest config).
→ Action: `gh release create v1.2.1 --notes "..."`. ✅
